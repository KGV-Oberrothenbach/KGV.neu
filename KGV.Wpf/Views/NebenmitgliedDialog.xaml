<Window x:Class="KGV.Views.NebenmitgliedDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStartupLocation="CenterOwner"
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize"
        Title="Nebenmitglied anlegen">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="260" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Vorname:" VerticalAlignment="Center" Margin="0,0,10,0" />
        <TextBox Grid.Row="0" Grid.Column="1" x:Name="VornameTextBox" />

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Nachname:" VerticalAlignment="Center" Margin="0,10,10,0" />
        <TextBox Grid.Row="1" Grid.Column="1" x:Name="NachnameTextBox" Margin="0,10,0,0" />

        <CheckBox Grid.Row="2" Grid.ColumnSpan="2" x:Name="AdresseUebernehmenCheckBox" Margin="0,12,0,0"
                  Content="Adresse vom Hauptmitglied übernehmen" IsChecked="True" />

        <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Width="100" Margin="0,0,10,0" Content="Abbrechen" IsCancel="True" />
            <Button Width="100" Content="OK" IsDefault="True" Click="Ok_Click" />
        </StackPanel>
    </Grid>
</Window>
