<UserControl x:Class="KGV.Views.VereinskonfigurationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource KgvBackgroundBrush}">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="30">
            <TextBlock Text="{Binding Titel}" FontSize="24" FontWeight="Bold" />
            <TextBlock Margin="0,6,0,0" Text="{Binding Untertitel}" FontSize="16" FontWeight="SemiBold" />
            <TextBlock Margin="0,8,0,16" Text="{Binding Beschreibung}" TextWrapping="Wrap" />

            <GroupBox Header="Vereinsdaten">
                <Grid Margin="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="220" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Margin="0,0,12,10" VerticalAlignment="Center" Text="Vereinsname" FontWeight="SemiBold" />
                    <TextBox Grid.Row="0" Grid.Column="1" Margin="0,0,0,10" Text="{Binding Vereinsname, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsEditable}" />

                    <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,0,12,10" VerticalAlignment="Center" Text="Kurzname" FontWeight="SemiBold" />
                    <TextBox Grid.Row="1" Grid.Column="1" Margin="0,0,0,10" Text="{Binding Kurzname, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsEditable}" />

                    <TextBlock Grid.Row="2" Grid.Column="0" Margin="0,0,12,10" VerticalAlignment="Center" Text="Registerangabe" FontWeight="SemiBold" />
                    <TextBox Grid.Row="2" Grid.Column="1" Margin="0,0,0,10" Text="{Binding Registerangabe, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsEditable}" />

                    <TextBlock Grid.Row="3" Grid.Column="0" Margin="0,0,12,10" VerticalAlignment="Center" Text="Straße" FontWeight="SemiBold" />
                    <TextBox Grid.Row="3" Grid.Column="1" Margin="0,0,0,10" Text="{Binding Strasse, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsEditable}" />

                    <TextBlock Grid.Row="4" Grid.Column="0" Margin="0,0,12,10" VerticalAlignment="Center" Text="PLZ" FontWeight="SemiBold" />
                    <TextBox Grid.Row="4" Grid.Column="1" Margin="0,0,0,10" Text="{Binding Plz, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsEditable}" />

                    <TextBlock Grid.Row="5" Grid.Column="0" Margin="0,0,12,10" VerticalAlignment="Center" Text="Ort" FontWeight="SemiBold" />
                    <TextBox Grid.Row="5" Grid.Column="1" Margin="0,0,0,10" Text="{Binding Ort, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsEditable}" />

                    <TextBlock Grid.Row="6" Grid.Column="0" Margin="0,0,12,10" VerticalAlignment="Center" Text="Standard-E-Mail" FontWeight="SemiBold" />
                    <TextBox Grid.Row="6" Grid.Column="1" Margin="0,0,0,10" Text="{Binding StandardEmail, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsEditable}" />

                    <TextBlock Grid.Row="7" Grid.Column="0" Margin="0,0,12,0" VerticalAlignment="Center" Text="Standard-Telefon" FontWeight="SemiBold" />
                    <TextBox Grid.Row="7" Grid.Column="1" Text="{Binding StandardTelefon, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsEditable}" />
                </Grid>
            </GroupBox>

            <GroupBox Header="Web und Bank" Margin="0,16,0,0">
                <Grid Margin="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="220" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Margin="0,0,12,10" VerticalAlignment="Center" Text="Website" FontWeight="SemiBold" />
                    <TextBox Grid.Row="0" Grid.Column="1" Margin="0,0,0,10" Text="{Binding Website, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsEditable}" />

                    <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,0,12,10" VerticalAlignment="Center" Text="Kontoinhaber" FontWeight="SemiBold" />
                    <TextBox Grid.Row="1" Grid.Column="1" Margin="0,0,0,10" Text="{Binding Kontoinhaber, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsEditable}" />

                    <TextBlock Grid.Row="2" Grid.Column="0" Margin="0,0,12,10" VerticalAlignment="Center" Text="Bankname" FontWeight="SemiBold" />
                    <TextBox Grid.Row="2" Grid.Column="1" Margin="0,0,0,10" Text="{Binding Bankname, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsEditable}" />

                    <TextBlock Grid.Row="3" Grid.Column="0" Margin="0,0,12,10" VerticalAlignment="Center" Text="IBAN" FontWeight="SemiBold" />
                    <TextBox Grid.Row="3" Grid.Column="1" Margin="0,0,0,10" Text="{Binding Iban, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsEditable}" />

                    <TextBlock Grid.Row="4" Grid.Column="0" Margin="0,0,12,0" VerticalAlignment="Center" Text="BIC" FontWeight="SemiBold" />
                    <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding Bic, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsEditable}" />
                </Grid>
            </GroupBox>

            <GroupBox Header="Dokumente und Datenschutz" Margin="0,16,0,0">
                <StackPanel Margin="12">
                    <TextBlock Text="Verwendungszweck Mitgliedsantrag" FontWeight="SemiBold" />
                    <TextBox Margin="0,4,0,10" Text="{Binding VerwendungszweckMitgliedsantrag, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsEditable}" />

                    <TextBlock Text="Verwendungszweck Pachtvertrag" FontWeight="SemiBold" />
                    <TextBox Margin="0,4,0,10" Text="{Binding VerwendungszweckPachtvertrag, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsEditable}" />

                    <TextBlock Text="Dokument-Ort" FontWeight="SemiBold" />
                    <TextBox Margin="0,4,0,10" Text="{Binding DokumentOrt, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsEditable}" />

                    <TextBlock Text="Standard-Hinweistext" FontWeight="SemiBold" />
                    <TextBox Margin="0,4,0,10"
                             Height="90"
                             AcceptsReturn="True"
                             TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto"
                             Text="{Binding StandardHinweistext, UpdateSourceTrigger=PropertyChanged}"
                             IsEnabled="{Binding IsEditable}" />

                    <TextBlock Text="Datenschutz-Text" FontWeight="SemiBold" />
                    <TextBox Margin="0,4,0,10"
                             Height="140"
                             AcceptsReturn="True"
                             TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto"
                             Text="{Binding DatenschutzText, UpdateSourceTrigger=PropertyChanged}"
                             IsEnabled="{Binding IsEditable}" />

                    <TextBlock Text="Datenschutz-Version" FontWeight="SemiBold" />
                    <TextBox Margin="0,4,0,10" Text="{Binding DatenschutzVersion, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsEditable}" />

                    <TextBlock Text="Datenschutz-Stand" FontWeight="SemiBold" />
                    <TextBox Margin="0,4,0,0" Text="{Binding DatenschutzStand, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding IsEditable}" ToolTip="Optional, z. B. 31.12.2025" />
                </StackPanel>
            </GroupBox>

            <TextBlock Margin="0,16,0,10"
                       Text="{Binding StatusMessage}"
                       Foreground="DarkSlateBlue"
                       TextWrapping="Wrap"
                       Visibility="{Binding HasStatusMessage, Converter={StaticResource BooleanToVisibilityConverter}}" />

            <StackPanel Margin="0,0,0,8" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Width="160" Height="34" Margin="0,0,8,0" Content="Aktualisieren" Command="{Binding RefreshCommand}" />
                <Button Width="220" Height="34" Content="Vereinskonfiguration speichern" Command="{Binding SaveCommand}" />
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</UserControl>
