<Window x:Class="KGV.Views.UpdatePromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Update verfügbar"
        Width="560"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="#FFF7F9FC">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="16" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="16" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="20" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Neue Version verfügbar"
                       FontSize="24"
                       FontWeight="SemiBold" />
            <TextBlock x:Name="IntroTextBlock"
                       Margin="0,10,0,0"
                       TextWrapping="Wrap"
                       FontSize="14" />
        </StackPanel>

        <Border Grid.Row="2"
                Padding="16"
                Background="White"
                BorderBrush="#D8E0EA"
                BorderThickness="1"
                CornerRadius="12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="170" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="10" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="10" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="10" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Installierte Version:" FontWeight="SemiBold" />
                <TextBlock Grid.Row="0" Grid.Column="1" x:Name="CurrentVersionTextBlock" TextWrapping="Wrap" />

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Verfügbare Version:" FontWeight="SemiBold" />
                <TextBlock Grid.Row="2" Grid.Column="1" x:Name="NewVersionTextBlock" TextWrapping="Wrap" />

                <TextBlock Grid.Row="4" Grid.Column="0" Text="Veröffentlicht:" FontWeight="SemiBold" />
                <TextBlock Grid.Row="4" Grid.Column="1" x:Name="PublishedAtTextBlock" TextWrapping="Wrap" />

                <TextBlock Grid.Row="6" Grid.Column="0" Text="Update-Typ:" FontWeight="SemiBold" />
                <TextBlock Grid.Row="6" Grid.Column="1" x:Name="MandatoryTextBlock" TextWrapping="Wrap" />
            </Grid>
        </Border>

        <Border Grid.Row="4"
                Padding="16"
                Background="White"
                BorderBrush="#D8E0EA"
                BorderThickness="1"
                CornerRadius="12">
            <StackPanel>
                <TextBlock Text="Änderungen"
                           FontSize="16"
                           FontWeight="SemiBold" />
                <TextBlock x:Name="NotesTextBlock"
                           Margin="0,10,0,0"
                           TextWrapping="Wrap" />
            </StackPanel>
        </Border>

        <Grid Grid.Row="6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="12" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                       x:Name="FooterTextBlock"
                       VerticalAlignment="Center"
                       Foreground="#4E5D6C"
                       TextWrapping="Wrap" />

            <Button Grid.Column="1"
                    x:Name="LaterButton"
                    MinWidth="110"
                    Height="36"
                    Padding="18,0"
                    Content="Später"
                    Click="LaterButton_Click" />

            <Button Grid.Column="3"
                    x:Name="InstallButton"
                    MinWidth="150"
                    Height="36"
                    Padding="18,0"
                    Content="Update installieren"
                    Click="InstallButton_Click" />
        </Grid>
    </Grid>
</Window>